version: '3'

services:
  fastify-app:
    build: .
    container_name: fastify-app
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    ports:
      - '3001:3001'
    environment:
      API_KEY: ${API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
