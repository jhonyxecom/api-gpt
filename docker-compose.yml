services:
  app:
    image: node:latest
    volumes:
      - bk_api_gpt:/app/
    ports:
      - "3001:3001"
    command: >
      sh -c "npm install &&
             npm run build &&
             npm start"
    environment:
      NODE_ENV: production
   
volumes:
  bg_api_gpt:

