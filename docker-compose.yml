services:
  app:
    image: node:latest
    working_dir: /app/
    volumes:
      - app
    ports:
      - "3001:3001"
    command: >
      sh -c "npm install &&
             npm run build &&
             npm start"
    environment:
      NODE_ENV: production
   
volumes:
  festfy_data:

