version: '3.8'

services:
  app:
    image: node:latest
    container_name: festfy-app
    working_dir: /app
    volumes:
      - festfy_data:/app/data
    ports:
      - "3001:3001"
    command: >
      sh -c "npm install && npm run build && npm start"
    environment:
      NODE_ENV: production

volumes:
  festfy_data:
